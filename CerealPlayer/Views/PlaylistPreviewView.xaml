<Window x:Class="CerealPlayer.Views.PlaylistPreviewView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CerealPlayer.Views"
        mc:Ignorable="d"
        Title="ActivePlaylists"
        Height="450"
        Width="400"
        WindowStyle="ToolWindow">
    <Grid Margin="{StaticResource DialogBorder}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox
            Grid.Row="0"
            ItemsSource="{Binding PlaylistItems}"
            SelectedItem="{Binding SelectedPlaylist}"
            Margin="{StaticResource BottomBorder}"/>

        <StackPanel 
            Grid.Row="1"
            Orientation="Horizontal" 
            HorizontalAlignment="Right">
            <Button 
                Content="New ActivePlaylist"
                Margin="{StaticResource RightBorder}"
                Command="{Binding NewPlaylistCommand}"/>
            <Button 
                Content="Cancel"
                Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
